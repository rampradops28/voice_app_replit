// Minimal Toaster stub – renders nothing for now
// You are using a custom toast store in `src/hooks/UseToast.jsx`.
// We can later render a viewport that maps over `toasts` from that store.
export function Toaster() {
  return null
}
